// ============ notes/note-17.js — ЭСКАЛАЦИЯ ПРИВИЛЕГИЙ В LINUX ============

KERNEL_DATA.addNote({
    id: 17,
    section: 'notes',
    title: 'Эскалация привилегий в Linux: основы',
    desc: 'SUID-бинарники, ошибки в sudo, cron-задачи от root, чтение прав через find — типичный чек-лист для получения root на скомпрометированной машине.',
    tags: ['Linux', 'privesc', 'CTF', 'практика'],
    date: 'Август 2026',
    content: function() {
        return [
            '<h3>Что такое эскалация привилегий</h3>',
            '<p>Ситуация: у вас уже есть доступ к системе от обычного пользователя (получен, например, через уязвимость веб-приложения), но нужны права root. Эскалация привилегий — поиск способа их получить за счёт ошибок конфигурации, а не новой уязвимости в самой ОС.</p>',

            '<h3>1. SUID-бинарники</h3>',
            '<p>Бит SUID заставляет программу выполняться с правами <em>владельца файла</em>, а не запустившего её пользователя. Если владелец — root, а сама программа позволяет выполнить произвольную команду (даже непреднамеренно) — это прямой путь к root.</p>',
            App.createCodeBlock(
                'find / -perm -4000 -type f 2>/dev/null   # все SUID-бинарники в системе',
                'bash'
            ),
            '<p>Дальше результат сверяется со списком известных бинарников, которые можно использовать для побега в root-шелл — такой справочник называется <strong>GTFOBins</strong> (<a href="https://gtfobins.github.io" target="_blank">gtfobins.github.io</a>). Например, если <code>find</code> имеет SUID:</p>',
            App.createCodeBlock('find . -exec /bin/sh -p \\; -quit', 'bash'),

            '<h3>2. Неправильно настроенный sudo</h3>',
            App.createCodeBlock('sudo -l   # что текущему пользователю разрешено запускать через sudo', 'bash'),
            '<p>Если в списке разрешённых команд есть редактор, интерпретатор или архиватор без ограничения аргументов — часто можно вырваться в root-шелл прямо из него (тот же принцип, что и с GTFOBins, только через sudo, а не SUID).</p>',
            App.createCodeBlock(
                '# Пример: если разрешено sudo vim без ограничений\nsudo vim -c \':!/bin/sh\'',
                'bash'
            ),

            '<h3>3. Cron-задачи, выполняемые от root</h3>',
            '<p>Если задача в crontab запускает скрипт, доступный для записи текущему пользователю, — можно подменить его содержимое и дождаться следующего запуска по расписанию.</p>',
            App.createCodeBlock(
                'cat /etc/crontab\nls -la /etc/cron.d/\nfind / -writable -type f 2>/dev/null | grep -v "^/proc"   # файлы, доступные на запись',
                'bash'
            ),

            '<h3>4. Устаревшее ядро с известной уязвимостью</h3>',
            App.createCodeBlock('uname -a   # версия ядра — проверить по базе известных CVE', 'bash'),
            '<p>Некоторые версии ядра Linux содержат баги, позволяющие получить root напрямую (например, семейство уязвимостей Dirty COW и подобные). Это уже не ошибка конфигурации, а уязвимость самого ядра — обновление системы закрывает такие пути.</p>',

            '<h3>5. Пароли и ключи в файловой системе</h3>',
            App.createCodeBlock(
                'grep -r "password" /etc/ 2>/dev/null\nfind / -name "*.bak" -o -name "id_rsa" 2>/dev/null\ncat ~/.bash_history   # иногда содержит команды с паролями в открытом виде',
                'bash'
            ),

            '<h3>Автоматизация чек-листа</h3>',
            '<p>Все перечисленные проверки вручную занимают время — на практике используют готовые скрипты-аудиторы:</p>',
            '<ul>',
            '<li><strong>LinPEAS</strong> — автоматически проверяет десятки типовых векторов эскалации и подсвечивает находки цветом.</li>',
            '<li><strong>linux-exploit-suggester</strong> — сопоставляет версию ядра с известными эксплойтами.</li>',
            '</ul>',
            '<p>Важно понимать логику вручную (как в этом конспекте), а не просто запускать автоматизацию — на экзамене или собеседовании спросят именно про принцип, а не про название скрипта.</p>',

            '<h3>Базовая защита (взгляд администратора)</h3>',
            '<ul>',
            '<li>Минимизировать количество SUID-бинарников, регулярно их аудировать.</li>',
            '<li>Настраивать sudo максимально узко — конкретная команда с конкретными аргументами, не «всё подряд».</li>',
            '<li>Проверять права на файлы, задействованные в cron-задачах, выполняемых от root.</li>',
            '<li>Своевременно обновлять ядро и следить за CVE.</li>',
            '</ul>',

            '<h3>Чек-лист: что вы должны уметь</h3>',
            '<ul>',
            '<li>Найти все SUID-бинарники в системе и проверить их по GTFOBins.</li>',
            '<li>Прочитать вывод <code>sudo -l</code> и понять, есть ли там путь к эскалации.</li>',
            '<li>Объяснить, почему запись в файл, который выполняется от root по расписанию, — это уязвимость.</li>',
            '</ul>',

            '<hr style="border:1px solid var(--border);margin:24px 0;">',
            '<p>Практика: <a href="javascript:void(0)" onclick="App.openNote(\'practice\', 110)">Лабораторная — чек-лист эскалации привилегий на своей ВМ</a>.</p>'
        ].join('');
    }
});
