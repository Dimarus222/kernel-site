// ============ notes/note-9.js — GIT ДЛЯ ПЕРВОКУРСНИКА ============

KERNEL_DATA.addNote({
    id: 9,
    section: 'notes',
    title: 'Git: система контроля версий с нуля',
    desc: 'Зачем нужен git, базовые команды, ветки, конфликты, работа с GitHub, .gitignore и типичные ошибки новичков.',
    tags: ['Git', 'база', 'GitHub', 'подготовка'],
    date: 'Август 2026',
    content: function() {
        return [
            '<h3>Зачем это нужно</h3>',
            '<p>Git — стандарт де-факто для хранения истории изменений кода. Пригодится не только на парах по разработке, но и для ведения собственных учебных проектов, курсовых, лабораторных — вместо файлов «проект_финал_v3_ИСПРАВЛЕННЫЙ.zip».</p>',

            '<h3>1. Базовая настройка</h3>',
            App.createCodeBlock(
                'git config --global user.name "Ваше Имя"\ngit config --global user.email "you@example.com"\ngit config --global --list   # проверить, что применилось',
                'bash'
            ),

            '<h3>2. Создание и клонирование репозитория</h3>',
            App.createCodeBlock(
                '# Начать новый репозиторий в текущей папке\ngit init\n\n# Скачать существующий с GitHub (включая скрытую папку .git —\n# именно она связывает папку с удалённым репозиторием)\ngit clone https://github.com/user/repo.git',
                'bash'
            ),

            '<h3>3. Основной цикл: добавить → закоммитить → отправить</h3>',
            App.createCodeBlock(
                'git status                  # что изменилось\ngit add file.py             # подготовить файл к коммиту\ngit add .                   # подготовить всё изменённое\ngit commit -m "Сообщение"   # зафиксировать изменения локально\ngit push origin main        # отправить на GitHub',
                'bash'
            ),
            '<p><strong>Хорошее сообщение коммита</strong> — короткое и по делу: что изменилось и зачем, а не «фикс» или «ещё правки».</p>',

            '<h3>4. Аутентификация: токен вместо пароля</h3>',
            '<p>С 2021 года GitHub не принимает обычный пароль аккаунта для push по HTTPS. Нужен <strong>Personal Access Token</strong>: Settings → Developer settings → Personal access tokens → Generate new token, с правом <code>repo</code>. При push токен вводится вместо пароля.</p>',

            '<h3>5. Ветки (branches)</h3>',
            '<p>Ветка — независимая линия разработки. Позволяет пробовать изменения, не ломая основную (обычно <code>main</code>) версию проекта.</p>',
            App.createCodeBlock(
                'git branch                       # список веток\ngit branch feature-login          # создать новую ветку\ngit checkout feature-login        # переключиться на неё\ngit checkout -b feature-login     # создать и сразу переключиться\n\n# После того как изменения готовы — слить обратно в main\ngit checkout main\ngit merge feature-login',
                'bash'
            ),

            '<h3>6. Конфликты слияния</h3>',
            '<p>Возникают, когда один и тот же участок файла изменён по-разному в двух ветках. Git не может решить сам — покажет маркеры прямо в файле:</p>',
            App.createCodeBlock(
                '<<<<<<< HEAD\nваш вариант строки\n=======\nвариант из другой ветки\n>>>>>>> feature-login',
                'plaintext'
            ),
            '<p>Нужно вручную отредактировать файл, оставив нужный вариант (или объединив оба), удалить маркеры <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code> / <code>=======</code> / <code>&gt;&gt;&gt;&gt;&gt;&gt;&gt;</code>, затем:</p>',
            App.createCodeBlock('git add file.py\ngit commit', 'bash'),

            '<h3>7. .gitignore — что не стоит коммитить</h3>',
            '<p>Файл со списком того, что git должен игнорировать: временные файлы, виртуальные окружения, пароли и ключи.</p>',
            App.createCodeBlock(
                '# .gitignore\n__pycache__/\n*.pyc\n.env\nvenv/\n.DS_Store\n*.log',
                'plaintext'
            ),
            '<p><strong>Никогда не коммитьте пароли, токены и ключи API</strong> — даже если потом удалить файл, он остаётся в истории коммитов. Если это случилось — токен нужно немедленно отозвать на GitHub и сгенерировать новый.</p>',

            '<h3>Типичные ошибки новичков</h3>',
            '<ul>',
            '<li><strong>git clone поверх существующей папки с файлами</strong> — рекомендуется клонировать пустой (или пока пустой) репозиторий, а затем копировать файлы в него, а не наоборот.</li>',
            '<li><strong>Коммит без <code>git add</code></strong> — git фиксирует только то, что явно добавлено в индекс.</li>',
            '<li><strong>Прямые кавычки заменяются «красивыми»</strong> при копировании с телефона или из некоторых редакторов — терминал перестаёт понимать команду и зависает на приглашении <code>&gt;</code>. Решение — Ctrl+C и ввод вручную.</li>',
            '<li><strong>Пробелы и скобки в путях без кавычек</strong> — команда вида <code>cd Мои Документы</code> или <code>unzip file (1).zip</code> без кавычек ломается. Берите такие пути в кавычки: <code>cd "Мои Документы"</code>.</li>',
            '</ul>',

            '<h3>Чек-лист: что вы должны уметь</h3>',
            '<ul>',
            '<li>Склонировать репозиторий и отправить в него изменения.</li>',
            '<li>Создать ветку, поработать в ней и слить обратно в main.</li>',
            '<li>Разрешить простой конфликт слияния вручную.</li>',
            '<li>Настроить .gitignore, чтобы случайно не закоммитить секреты.</li>',
            '</ul>',

            '<hr style="border:1px solid var(--border);margin:24px 0;">',
            '<p>Практика: <a href="javascript:void(0)" onclick="App.openNote(\'practice\', 104)">Лабораторная — рабочий цикл Git</a>.</p>'
        ].join('');
    }
});
