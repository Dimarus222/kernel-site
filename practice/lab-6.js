// ============ practice/lab-6.js — ЛАБОРАТОРНАЯ: RSA ВРУЧНУЮ ============

KERNEL_DATA.addPractice({
    id: 106,
    section: 'practice',
    title: 'Лабораторная: RSA вручную на малых числах',
    desc: 'Генерируем пару ключей, шифруем и расшифровываем сообщение на маленьких простых числах — чтобы увидеть математику RSA изнутри.',
    tags: ['практика', 'RSA', 'криптография', 'Python'],
    date: 'Август 2026',
    content: function() {
        return [
            '<h3>Цель</h3>',
            '<p>Пройти весь алгоритм RSA руками на маленьких числах, чтобы понимать, что происходит внутри библиотек вроде OpenSSL. Теория — в конспекте <a href="javascript:void(0)" onclick="App.openNote(\'notes\', 8)">«Криптография: основы»</a>.</p>',
            '<p><strong>Важно:</strong> числа здесь учебные (единицы-десятки), в реальном RSA используются простые числа на сотни цифр. На таких маленьких числах алгоритм взламывается перебором за секунды — это нормально для учебной цели, но так не шифруют ничего реального.</p>',

            '<h3>Шаг 1. Выбираем два простых числа</h3>',
            App.createCodeBlock(
                'p = 61\nq = 53\nn = p * q          # публичный модуль\nprint("n =", n)     # 3233',
                'python'
            ),

            '<h3>Шаг 2. Считаем функцию Эйлера и выбираем открытую экспоненту</h3>',
            App.createCodeBlock(
                'phi = (p - 1) * (q - 1)\nprint("phi =", phi)   # 3120\n\n# e должно быть взаимно простым с phi. 17 — стандартный учебный выбор\ne = 17\nimport math\nprint(math.gcd(e, phi))   # должно быть 1',
                'python'
            ),

            '<h3>Шаг 3. Находим приватную экспоненту</h3>',
            '<p>Нужно найти <code>d</code> такое, что <code>(d * e) mod phi == 1</code> — это называется обратным элементом по модулю.</p>',
            App.createCodeBlock(
                'from sympy import mod_inverse\nd = mod_inverse(e, phi)\nprint("d =", d)   # 2753\n\n# TODO: проверьте вручную, что (d * e) % phi == 1',
                'python'
            ),
            '<details style="margin:12px 0;"><summary style="cursor:pointer;color:var(--accent);">Показать проверку</summary>',
            App.createCodeBlock('print((d * e) % phi)  # 1', 'python'),
            '</details>',

            '<h3>Шаг 4. Шифруем сообщение</h3>',
            '<p>Публичный ключ — пара <code>(e, n)</code>. Шифрование: <code>c = m^e mod n</code>, где m — число меньше n.</p>',
            App.createCodeBlock(
                'message = 65   # например, код символа \'A\'\nencrypted = pow(message, e, n)\nprint("Зашифровано:", encrypted)',
                'python'
            ),

            '<h3>Шаг 5. Расшифровываем</h3>',
            '<p>Приватный ключ — пара <code>(d, n)</code>. Расшифровка: <code>m = c^d mod n</code>.</p>',
            App.createCodeBlock(
                'decrypted = pow(encrypted, d, n)\nprint("Расшифровано:", decrypted)\nassert decrypted == message',
                'python'
            ),

            '<h3>Шаг 6. Задание на закрепление</h3>',
            '<p>Возьмите другую пару простых чисел (например, p=17, q=23) и повторите весь процесс самостоятельно — от выбора e до успешной расшифровки.</p>',

            '<h3>Самопроверка</h3>',
            '<ul>',
            '<li>Расшифрованное сообщение полностью совпадает с исходным.</li>',
            '<li>Вы можете объяснить своими словами, почему знание <code>n</code> и <code>e</code> (публичный ключ) не позволяет быстро вычислить <code>d</code> без разложения <code>n</code> на множители.</li>',
            '<li>Вы понимаете, почему в реальном RSA простые числа выбираются длиной в сотни цифр.</li>',
            '</ul>'
        ].join('');
    }
});
