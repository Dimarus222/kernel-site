// ============ practice/lab-7.js — ЛАБОРАТОРНАЯ: СТАТИЧЕСКИЙ АНАЛИЗ БИНАРНИКА ============

KERNEL_DATA.addPractice({
    id: 107,
    section: 'practice',
    title: 'Лабораторная: статический анализ простого бинарника',
    desc: 'Компилируем собственный crackme на C, находим в нём условие проверки пароля через strings и objdump, не запуская отладчик.',
    tags: ['практика', 'reverse', 'CTF'],
    date: 'Август 2026',
    content: function() {
        return [
            '<h3>Цель</h3>',
            '<p>Написать простую программу-пароль на C, скомпилировать её и «взломать» собственными руками через статический анализ — без исходного кода перед глазами. Теория — в конспекте <a href="javascript:void(0)" onclick="App.openNote(\'notes\', 12)">«Реверс-инжиниринг и ассемблер x86»</a>.</p>',

            '<h3>Шаг 1. Пишем и компилируем crackme</h3>',
            App.createCodeBlock(
                '// crackme.c\n#include <stdio.h>\n#include <string.h>\n\nint main() {\n    char password[20];\n    printf("Введите пароль: ");\n    scanf("%19s", password);\n\n    if (strcmp(password, "kernel2026") == 0) {\n        printf("Доступ разрешён\\n");\n    } else {\n        printf("Неверный пароль\\n");\n    }\n    return 0;\n}',
                'c'
            ),
            App.createCodeBlock('gcc -o crackme crackme.c\n./crackme   # убедитесь, что программа работает как ожидается', 'bash'),

            '<h3>Шаг 2. Забываем пароль и ищем его статически</h3>',
            '<p>Представьте, что вы не знаете пароль — только скомпилированный файл <code>crackme</code>. Попробуйте найти его через:</p>',
            App.createCodeBlock('strings crackme | less', 'bash'),
            '<p>Так как <code>strcmp</code> сравнивает с обычной строкой в памяти, она попадёт в вывод <code>strings</code> почти всегда открытым текстом.</p>',

            '<h3>Шаг 3. Смотрим на месте ли сравнение через дизассемблер</h3>',
            App.createCodeBlock('objdump -d crackme | grep -A 20 "<main>:"', 'bash'),
            '<p>Найдите вызов <code>call strcmp</code> (или <code>call &lt;strcmp@plt&gt;</code>) — непосредственно перед ним обычно кладётся адрес строки для сравнения через <code>lea</code>/<code>mov</code>. Сопоставьте адрес строки с выводом <code>strings -t x crackme</code> (флаг <code>-t x</code> покажет смещение каждой строки в шестнадцатеричном виде).</p>',

            '<h3>Шаг 4. Усложняем задачу самостоятельно</h3>',
            '<p>Перепишите <code>crackme.c</code> так, чтобы пароль не хранился открытым текстом, а проверялся посимвольно через XOR с ключом:</p>',
            App.createCodeBlock(
                'char input[20];\nchar key = 0x2A;\nchar expected[] = {0x4B^0x2A, 0x32^0x2A, 0x30^0x2A, 0x32^0x2A, 0x36^0x2A, 0}; // пример\n// TODO: реализуйте посимвольную проверку через XOR и попробуйте найти пароль\n// снова через strings — заметите разницу?',
                'c'
            ),
            '<p>После такого изменения простой <code>strings</code> уже не выдаст пароль напрямую — придётся либо дизассемблировать логику XOR, либо перейти к динамическому анализу через gdb.</p>',

            '<h3>Самопроверка</h3>',
            '<ul>',
            '<li>Вы нашли пароль первой версии crackme через <code>strings</code> без запуска программы под отладчиком.</li>',
            '<li>Вы видите в выводе <code>objdump -d</code> вызов <code>strcmp</code> и можете указать, где именно происходит сравнение.</li>',
            '<li>Вы понимаете, почему версия с XOR сложнее для статического анализа «в лоб».</li>',
            '</ul>'
        ].join('');
    }
});
